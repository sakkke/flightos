name: Main
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup
        run: |
          COMMIT_MESSAGE="$(head -n 1 <<< "${{ github.event.head_commit.message }}")"
          echo COMMIT_MESSAGE="$COMMIT_MESSAGE" >> "$GITHUB_ENV"
          echo RELEASE_VERSION="${COMMIT_MESSAGE:14}" >> "$GITHUB_ENV"
      - name: Build
        run: make
      - name: Test
        run: make check
      - name: Tag
        run: |
          git tag "$RELEASE_VERSION"
          make release ver="$RELEASE_VERSION"
        if: "startsWith(github.event.head_commit.message, 'chore(draft): ')"
      - name: Build
        run: make
      - name: Pack
        run: cd ./dist && tar -acf flightos-"$RELEASE_VERSION".tar.gz flightos
        if: startsWith(github.ref, 'refs/tags/')
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: dist/flightos-${{ env.RELEASE_VERSION }}.tar.gz
          generate_release_notes: true
